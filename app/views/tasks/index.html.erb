<h1>My tasks</h1>

<% @tasks.each do|task| %>
  <li><%= link_to task.title, task_path(task) %></li>
  <%= link_to "delete 🗑", delete_path(task), method: :delete, data: { confirm: "Are you sure to delete #{task.title}?" }%>
  <br>
  <% end %>

<%= link_to "Add new task", tasks_new_path %>
